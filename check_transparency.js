const fs = require('fs');
// Very simple check: read few bytes off a PNG to see if it's RGBA
// Better yet, just use a simple script that prints out the colors of the corners using a quick python script.
